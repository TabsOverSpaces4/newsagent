# Pinned top-level dependencies. Transitive deps resolve from these.
# Local dev: uv venv --python 3.12 .venv && uv pip install -r requirements.txt
anthropic==0.121.0
feedparser==6.0.14
httpx==0.28.1
jinja2==3.1.6
numpy==2.5.2
pydantic==2.13.4
pyyaml==6.0.3
resend==2.35.0
sentence-transformers==5.7.0
torch==2.13.0
trafilatura==2.2.0

# Dev / CI tooling
pytest==9.1.1
ruff==0.16.2
